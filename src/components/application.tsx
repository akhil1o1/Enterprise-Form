import { FormContent } from "./formContent";

export const Application = () => {
   return (
      <section>
         <form noValidate>
            <FormContent />
         </form>
      </section>
   );
};

export default Application;
